package com.example.TeamAppDemo;

import org.junit.jupiter.api.Disabled;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
@Disabled("Disabled: we use focused @WebMvcTest; no need to boot full context here.")
class TeamAppDemoApplicationTests {
    // no tests here on purpose
}